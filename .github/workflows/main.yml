name: CLO_RDP_FINAL_FIX
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Enable Windows RDP & Create User
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          $user = "runneradmin"
          $pass = ConvertTo-SecureString "Admin123!" -AsPlainText -Force
          if (-not (Get-LocalUser -Name $user -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name $user -Password $pass -Description "RDP User"
              Add-LocalGroupMember -Group "Administrators" -Member $user
          } else {
              $existingUser = Get-LocalUser -Name $user
              $existingUser | Set-LocalUser -Password $pass
          }
      - name: Install Cloudflared
        run: |
          curl -L --output cloudflared.exe https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe
      - name: Start Cloudflare Tunnel
        run: |
          ./cloudflared.exe tunnel --url rdp://localhost:3389
      - name: Keep Alive
        run: |
          Write-Host "RDP is ready. Use Password: Admin123!"
          Start-Sleep -Seconds 21600
